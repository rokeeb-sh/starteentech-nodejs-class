{
  "name": "starteentech-nodejs-class",
  "version": "1.0.0",
  "description": "Classes on nodejs",
  "author": "Rokeeb Shobiye",
  "type": "module",
  "license": "ISC",
  "scripts": {
    "dev": "nodemon node.js",
    "start": "node main.js",
    "build": "echo \"Add a build command to run this\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
